Step 1: Add server certificate to /resources/certs in pem format<br>
Step 2: on WINDOWS: set APP_ENV environment variable and add config file to resources directory with name format: config-APP-ENV.properties<br>
